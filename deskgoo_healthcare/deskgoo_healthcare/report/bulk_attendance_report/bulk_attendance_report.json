{
 "add_total_row": 0,
 "add_translate_data": 0,
 "columns": [
  {
   "fieldname": "employee",
   "fieldtype": "Link",
   "label": "Employee",
   "options": "Attendance",
   "width": 130
  },
  {
   "fieldname": "employee_name",
   "fieldtype": "Data",
   "label": "Name",
   "width": 130
  },
  {
   "fieldname": "attendance_date",
   "fieldtype": "Date",
   "label": "Date",
   "options": "",
   "width": 100
  },
  {
   "fieldname": "attendance_day",
   "fieldtype": "Data",
   "label": "Day",
   "width": 80
  },
  {
   "fieldname": "status",
   "fieldtype": "Data",
   "label": "Status",
   "width": 70
  },
  {
   "fieldname": "shift",
   "fieldtype": "Data",
   "label": "Shift",
   "width": 150
  },
  {
   "fieldname": "in_time",
   "fieldtype": "Datetime",
   "label": "InTime",
   "width": 180
  },
  {
   "fieldname": "out_time",
   "fieldtype": "Datetime",
   "label": "OutTime",
   "width": 180
  },
  {
   "fieldname": "working_hours",
   "fieldtype": "Float",
   "label": "Hours",
   "width": 80
  },
  {
   "fieldname": "late_entry",
   "fieldtype": "Check",
   "label": "Late",
   "width": 50
  }
 ],
 "creation": "2025-12-22 18:31:06.605333",
 "disabled": 0,
 "docstatus": 0,
 "doctype": "Report",
 "filters": [
  {
   "default": "",
   "fieldname": "employee",
   "fieldtype": "Link",
   "label": "Employee",
   "mandatory": 0,
   "options": "Employee",
   "wildcard_filter": 0
  },
  {
   "fieldname": "from_date",
   "fieldtype": "Date",
   "label": "From",
   "mandatory": 1,
   "wildcard_filter": 0
  },
  {
   "fieldname": "to_date",
   "fieldtype": "Date",
   "label": "To",
   "mandatory": 1,
   "wildcard_filter": 0
  }
 ],
 "idx": 0,
 "is_standard": "Yes",
 "json": "{}",
 "letter_head": "Golden Hospital",
 "letterhead": null,
 "modified": "2025-12-22 19:06:15.152879",
 "modified_by": "Administrator",
 "module": "Deskgoo Healthcare",
 "name": "Bulk Attendance Report",
 "owner": "Administrator",
 "prepared_report": 0,
 "query": "import frappe\r\nfrom frappe import _\r\n\r\ndef execute(filters=None):\r\n    if not filters or not filters.get(\"from_date\") or not filters.get(\"to_date\"):\r\n        return [], []\r\n\r\n    columns = [\r\n        {\"label\": _(\"Employee\"), \"fieldname\": \"employee\", \"fieldtype\": \"Link\", \"options\": \"Employee\", \"width\": 120},\r\n        {\"label\": _(\"Employee Name\"), \"fieldname\": \"employee_name\", \"fieldtype\": \"Data\", \"width\": 200},\r\n        {\"label\": _(\"Date\"), \"fieldname\": \"attendance_date\", \"fieldtype\": \"Date\", \"width\": 100},\r\n        {\"label\": _(\"Day\"), \"fieldname\": \"attendance_day\", \"fieldtype\": \"Data\", \"width\": 100},\r\n        {\"label\": _(\"Status\"), \"fieldname\": \"status\", \"fieldtype\": \"Data\", \"width\": 100},\r\n        {\"label\": _(\"Shift\"), \"fieldname\": \"shift_type\", \"fieldtype\": \"Data\", \"width\": 100},\r\n        {\"label\": _(\"In Time\"), \"fieldname\": \"in_time\", \"fieldtype\": \"Datetime\", \"width\": 150},\r\n        {\"label\": _(\"Out Time\"), \"fieldname\": \"out_time\", \"fieldtype\": \"Datetime\", \"width\": 150},\r\n        {\"label\": _(\"Working Hours\"), \"fieldname\": \"working_hours\", \"fieldtype\": \"Float\", \"width\": 120, \"precision\": 3},\r\n        {\"label\": _(\"Late Entry\"), \"fieldname\": \"late_entry\", \"fieldtype\": \"Int\", \"width\": 100},\r\n    ]\r\n\r\n    query = \"\"\"\r\nWITH RECURSIVE DateRange AS (\r\n    SELECT %(from_date)s AS attendance_date\r\n    UNION ALL\r\n    SELECT DATE_ADD(attendance_date, INTERVAL 1 DAY)\r\n    FROM DateRange\r\n    WHERE attendance_date < %(to_date)s\r\n),\r\n-- Fetch all check-in/out records within date range for each active employee, including shift info\r\nCheckinRecords AS (\r\n    SELECT\r\n        ci.employee,\r\n        DATE(ci.time) AS attendance_date,\r\n        ci.time AS checkin_time,\r\n        ci.log_type,\r\n        ci.shift,\r\n        ci.shift_start,\r\n        ROW_NUMBER() OVER (PARTITION BY ci.employee, DATE(ci.time), ci.log_type ORDER BY ci.time) AS rn\r\n    FROM `tabEmployee Checkin` ci\r\n    INNER JOIN `tabEmployee` e ON ci.employee = e.employee AND e.status = 'Active'\r\n    WHERE DATE(ci.time) BETWEEN %(from_date)s AND %(to_date)s\r\n),\r\n-- Get all valid and active shift assignments for active employees\r\nShiftAssignments AS (\r\n    SELECT\r\n        sa.employee,\r\n        sa.shift_type,\r\n        sa.custom_start_time,\r\n        sa.custom_end_time,\r\n        sa.start_date\r\n    FROM `tabShift Assignment` sa\r\n    INNER JOIN `tabEmployee` e ON sa.employee = e.employee AND e.status = 'Active'\r\n    WHERE sa.status = 'Active'\r\n      AND sa.docstatus = 1\r\n      AND sa.start_date <= %(to_date)s\r\n),\r\n-- Match each IN check-in with the closest shift based on time difference\r\nMatchedShifts AS (\r\n    SELECT\r\n        employee,\r\n        checkin_time,\r\n        attendance_date,\r\n        shift_type,\r\n        shift_start,\r\n        shift_end\r\n    FROM (\r\n        SELECT\r\n            ci.employee,\r\n            ci.checkin_time,\r\n            ci.attendance_date,\r\n            sa.shift_type,\r\n            CASE\r\n                WHEN sa.custom_end_time >= sa.custom_start_time\r\n                THEN CONCAT(ci.attendance_date, ' ', sa.custom_start_time)\r\n                ELSE CONCAT(ci.attendance_date, ' ', sa.custom_start_time)\r\n            END AS shift_start,\r\n            CASE\r\n                WHEN sa.custom_end_time >= sa.custom_start_time\r\n                THEN CONCAT(ci.attendance_date, ' ', sa.custom_end_time)\r\n                ELSE CONCAT(DATE_ADD(ci.attendance_date, INTERVAL 1 DAY), ' ', sa.custom_end_time)\r\n            END AS shift_end,\r\n            ABS(TIMESTAMPDIFF(MINUTE,\r\n                CASE\r\n                    WHEN sa.custom_end_time >= sa.custom_start_time\r\n                    THEN CONCAT(ci.attendance_date, ' ', sa.custom_start_time)\r\n                    ELSE CONCAT(ci.attendance_date, ' ', sa.custom_start_time)\r\n                END,\r\n                ci.checkin_time\r\n            )) AS time_diff,\r\n            ROW_NUMBER() OVER (PARTITION BY ci.employee, ci.checkin_time ORDER BY \r\n                ABS(TIMESTAMPDIFF(MINUTE,\r\n                    CASE\r\n                        WHEN sa.custom_end_time >= sa.custom_start_time\r\n                        THEN CONCAT(ci.attendance_date, ' ', sa.custom_start_time)\r\n                        ELSE CONCAT(ci.attendance_date, ' ', sa.custom_start_time)\r\n                    END,\r\n                    ci.checkin_time\r\n                )) ASC, sa.shift_type ASC) AS rn\r\n        FROM CheckinRecords ci\r\n        LEFT JOIN ShiftAssignments sa\r\n            ON sa.employee = ci.employee\r\n            AND sa.start_date <= ci.attendance_date\r\n            AND (\r\n                ci.checkin_time >= DATE_SUB(\r\n                    CASE\r\n                        WHEN sa.custom_end_time >= sa.custom_start_time\r\n                        THEN CONCAT(ci.attendance_date, ' ', sa.custom_start_time)\r\n                        ELSE CONCAT(ci.attendance_date, ' ', sa.custom_start_time)\r\n                    END,\r\n                    INTERVAL 120 MINUTE\r\n                )\r\n                AND ci.checkin_time <=\r\n                    CASE\r\n                        WHEN sa.custom_end_time >= sa.custom_start_time\r\n                        THEN CONCAT(ci.attendance_date, ' ', sa.custom_end_time)\r\n                        ELSE CONCAT(DATE_ADD(ci.attendance_date, INTERVAL 1 DAY), ' ', sa.custom_end_time)\r\n                    END\r\n            )\r\n        WHERE ci.log_type = 'IN'\r\n    ) tmp\r\n    WHERE rn = 1\r\n),\r\n-- Pair each IN check-in with the next valid OUT check-in (no other IN between), within 2 days\r\nPairedINOUT AS (\r\n    SELECT\r\n        ci.employee,\r\n        ci.attendance_date,\r\n        ci.checkin_time AS in_time,\r\n        ms.shift_type,\r\n        ms.shift_start,\r\n        ms.shift_end,\r\n        MIN(o.checkin_time) AS out_time\r\n    FROM CheckinRecords ci\r\n    LEFT JOIN CheckinRecords o\r\n        ON o.employee = ci.employee\r\n        AND o.log_type = 'OUT'\r\n        AND o.checkin_time > ci.checkin_time\r\n        AND o.checkin_time <= DATE_ADD(ci.checkin_time, INTERVAL 2 DAY)\r\n        AND (\r\n            DATE(o.checkin_time) = ci.attendance_date\r\n            OR (\r\n                DATE(o.checkin_time) = DATE_ADD(ci.attendance_date, INTERVAL 1 DAY)\r\n                AND TIME(o.checkin_time) BETWEEN '06:00:00' AND '10:00:00'\r\n            )\r\n        )\r\n        AND NOT EXISTS (\r\n            SELECT 1\r\n            FROM CheckinRecords i2\r\n            WHERE i2.employee = ci.employee\r\n              AND i2.log_type = 'IN'\r\n              AND i2.checkin_time > ci.checkin_time\r\n              AND i2.checkin_time < o.checkin_time\r\n        )\r\n    LEFT JOIN MatchedShifts ms\r\n        ON ms.employee = ci.employee\r\n        AND ms.checkin_time = ci.checkin_time\r\n    WHERE ci.log_type = 'IN'\r\n    GROUP BY ci.employee, ci.checkin_time, ci.attendance_date, ms.shift_type, ms.shift_start, ms.shift_end\r\n),\r\n-- Find all OUT entries that couldn\u2019t be paired with any IN\r\nUnpairedOUT AS (\r\n    SELECT\r\n        o.employee,\r\n        o.attendance_date,\r\n        NULL AS in_time,\r\n        NULL AS shift_type,\r\n        NULL AS shift_start,\r\n        NULL AS shift_end,\r\n        o.checkin_time AS out_time\r\n    FROM CheckinRecords o\r\n    WHERE o.log_type = 'OUT'\r\n      AND NOT EXISTS (\r\n          SELECT 1\r\n          FROM PairedINOUT p\r\n          WHERE p.employee = o.employee\r\n            AND p.out_time = o.checkin_time\r\n      )\r\n),\r\n-- Combine Paired IN/OUT and unpaired OUTs\r\nCheckinPairs AS (\r\n    SELECT employee, attendance_date, in_time, shift_type, shift_start, shift_end, out_time\r\n    FROM PairedINOUT\r\n    UNION ALL\r\n    SELECT employee, attendance_date, in_time, shift_type, shift_start, shift_end, out_time\r\n    FROM UnpairedOUT\r\n),\r\n-- Generate present records when in_time is available (out_time may be null)\r\nPresentRecords AS (\r\n    SELECT\r\n        cp.employee,\r\n        emp.employee_name,\r\n        cp.attendance_date,\r\n        DAYNAME(cp.attendance_date) AS attendance_day,\r\n        'Present' AS status,\r\n        cp.shift_type,\r\n        cp.in_time,\r\n        cp.out_time,\r\n        ROUND(\r\n            CASE\r\n                WHEN cp.in_time IS NOT NULL AND cp.out_time IS NOT NULL\r\n                AND cp.out_time <= DATE_ADD(cp.in_time, INTERVAL 2 DAY)\r\n                THEN TIMESTAMPDIFF(SECOND, cp.in_time, cp.out_time) / 3600\r\n                ELSE 0\r\n            END, 3\r\n        ) AS working_hours,\r\n        CASE\r\n            WHEN cp.in_time IS NOT NULL THEN\r\n                CASE\r\n                    WHEN cp.shift_start IS NOT NULL\r\n                    THEN CASE\r\n                            WHEN TIMESTAMPDIFF(MINUTE, cp.shift_start, cp.in_time) > 15 THEN 1\r\n                            ELSE 0\r\n                         END\r\n                    ELSE CASE\r\n                            WHEN cp.in_time > CONCAT(cp.attendance_date, ' ', '09:00:00') THEN 1\r\n                            ELSE 0\r\n                         END\r\n                END\r\n            ELSE 0\r\n        END AS late_entry\r\n    FROM CheckinPairs cp\r\n    LEFT JOIN `tabEmployee` emp ON cp.employee = emp.employee\r\n    WHERE cp.in_time IS NOT NULL\r\n      AND cp.attendance_date = DATE(cp.in_time)\r\n),\r\n-- Employees with no IN records and no unpaired OUT records are considered absent\r\nAbsentRecords AS (\r\n    SELECT\r\n        emp.employee,\r\n        emp.employee_name,\r\n        dr.attendance_date,\r\n        DAYNAME(dr.attendance_date) AS attendance_day,\r\n        'Absent' AS status,\r\n        NULL AS shift_type,\r\n        NULL AS in_time,\r\n        NULL AS out_time,\r\n        0 AS working_hours,\r\n        0 AS late_entry\r\n    FROM DateRange dr\r\n    CROSS JOIN `tabEmployee` emp\r\n    LEFT JOIN CheckinPairs cp\r\n        ON emp.employee = cp.employee\r\n        AND cp.attendance_date = dr.attendance_date\r\n        AND cp.in_time IS NOT NULL\r\n    LEFT JOIN UnpairedOUT uo\r\n        ON emp.employee = uo.employee\r\n        AND uo.attendance_date = dr.attendance_date\r\n    WHERE emp.status = 'Active'\r\n      AND cp.employee IS NULL\r\n      AND uo.employee IS NULL\r\n),\r\n-- Unpaired OUT records marked as Present\r\nUnpairedOutRecords AS (\r\n    SELECT\r\n        uo.employee,\r\n        emp.employee_name,\r\n        uo.attendance_date,\r\n        DAYNAME(uo.attendance_date) AS attendance_day,\r\n        'Present' AS status,\r\n        NULL AS shift_type,\r\n        NULL AS in_time,\r\n        uo.out_time,\r\n        0 AS working_hours,\r\n        0 AS late_entry\r\n    FROM UnpairedOUT uo\r\n    LEFT JOIN `tabEmployee` emp ON uo.employee = emp.employee\r\n),\r\n-- Combine all present/absent/unpaired records\r\nAllRecords AS (\r\n    SELECT * FROM PresentRecords\r\n    UNION\r\n    SELECT * FROM AbsentRecords\r\n    UNION\r\n    SELECT * FROM UnpairedOutRecords\r\n),\r\n-- Add a per-employee total summary for working hours\r\nGrandTotal AS (\r\n    SELECT\r\n        employee,\r\n        CONCAT(MAX(employee_name), ' - Total') AS employee_name,\r\n        NULL AS attendance_date,\r\n        NULL AS attendance_day,\r\n        NULL AS status,\r\n        NULL AS shift_type,\r\n        NULL AS in_time,\r\n        NULL AS out_time,\r\n        SUM(working_hours) AS working_hours,\r\n        NULL AS late_entry,\r\n        1 AS is_total\r\n    FROM AllRecords\r\n    GROUP BY employee\r\n)\r\n-- Final output: daily records + per-employee total working hours\r\nSELECT employee, employee_name, attendance_date, attendance_day, status, shift_type, in_time, out_time, working_hours, late_entry\r\nFROM AllRecords\r\nUNION\r\nSELECT employee, employee_name, attendance_date, attendance_day, status, shift_type, in_time, out_time, working_hours, late_entry\r\nFROM GrandTotal\r\nORDER BY employee ASC, is_total ASC, attendance_date ASC\r\n    \"\"\"\r\n\r\n    data = frappe.db.sql(query, {\"from_date\": filters[\"from_date\"], \"to_date\": filters[\"to_date\"]}, as_dict=True)\r\n\r\n    return columns, data",
 "ref_doctype": "Attendance",
 "reference_report": "",
 "report_name": "Bulk Attendance Report",
 "report_type": "Query Report",
 "roles": [
  {
   "role": "System Manager"
  },
  {
   "role": "HR User"
  },
  {
   "role": "HR Manager"
  },
  {
   "role": "Employee"
  }
 ],
 "timeout": 0
}
